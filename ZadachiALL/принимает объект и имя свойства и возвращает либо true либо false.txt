Реализуйте функцию removeProperty, 
которая принимает объек и имя свойства и выполняет следующие действия:
Если объект obj имеет свойство prop, 
функция удаляет свойство из объекта и возвращает true; 
во всех остальных случаях возвращается false.
function removeProperty(obj, prop) {
 return null;
}
 */
 




function removeProperty(obj, prop) {
  return prop in obj ? (delete obj[prop], true) : false;
}

// removeProperty({},name);